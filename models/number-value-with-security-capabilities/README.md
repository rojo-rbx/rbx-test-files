# number-value-with-security-capabilities
* Roblox Studio version: 0.594.0.5940525

Contains a single `NumberValue` object that contains a `Capabilities` property (of type `SecurityCapabilities`) with a value of `0`.
